# german-audio/ ships with the repo as mp3; the wav masters do not
.DS_Store
german-audio/*.wav
german-audio/parts/*.wav
# the Polly endpoint for this copy of the Heft (see tools/tts-lambda/); not for the public repo
tts.json

# local preview server log
.server.log
